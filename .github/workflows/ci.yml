name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Enable Corepack and pnpm
        run: corepack pnpm@11.5.0 --version

      - name: Install dependencies
        env:
          CI: "false"
          npm_config_ignore_scripts: "false"
        run: |
          export npm_config_user_agent="pnpm/$(corepack pnpm@11.5.0 --version)"
          corepack pnpm@11.5.0 install --frozen-lockfile

      - name: Typecheck
        run: corepack pnpm@11.5.0 run typecheck

      - name: Build
        run: corepack pnpm@11.5.0 run build
